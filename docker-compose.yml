version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    environment:
      HOST_IP: ${HOST_IP}
      DISPLAY: ${DISPLAY}
      XAUTHORITY: ${XAUTHORITY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
